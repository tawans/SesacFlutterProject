import 'package:flutter/material.dart';

class LockerPage extends StatelessWidget {
  const LockerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('보관함 화면'),
      ),
    );
  }
}
